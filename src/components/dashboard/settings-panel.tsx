
interface TickDurationSliderProps {
  value?: number;
  onChange?: (val: number) => void;
}

export function TickDurationSlider({ value: _value, onChange: _onChange }: TickDurationSliderProps) {
  // Placeholder for the tick duration selector used in strategy panel settings
  return null;
}
